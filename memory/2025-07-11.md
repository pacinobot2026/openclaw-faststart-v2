# 2025-07-11 - Daily Journal

## Session Start

**Context:** Compaction just happened - lost previous conversation including 3 log files Chad shared about customer onboarding issues.

**Chad's point:** I should have journaled BEFORE/DURING, not relied on session memory. FAILED AGAIN at the core rule.

---

## Current Task: BOOTSTRAP.md Customer Issues

**Problem:** Custom BOOTSTRAP.md on customer systems is causing problems

**What Chad mentioned:**
- `openclaw config schema` doesn't exist (I documented wrong command?)
- 3 log files showing what happened on customer system - NEED TO RE-REQUEST
- BOOTSTRAP.md might be too rigid - breaks if customer goes off-script

**Issues I identified from reading BOOTSTRAP.md:**
1. Too script-like/rigid - no handling for off-script questions
2. No state tracking - can't resume if interrupted
3. Assumes perfect flow - real humans skip, ask questions, get confused
4. No recovery mechanism

**Log files received:** Yes - massive log dump from customer system

---

## Log Analysis - Customer System Issues

### Key Errors Found:

**1. `config.patch` failing repeatedly with "invalid config"**
```
[tools] gateway failed: invalid config
```
- Happened at: 00:10:49, 00:10:58, 00:11:15, 00:37:56
- Agent kept retrying but same error each time

**2. Missing documentation file**
```
ENOENT: no such file or directory, access 'configuration.md'
```
- Agent tried to read `openclaw\docs\configuration.md` but it doesn't exist
- This is when agent tried to figure out correct config format

**3. What the agent was trying to do:**
- Following BOOTSTRAP.md Step 1 (Claude API key)
- Using `gateway` tool with `action: config.patch`
- Passing JSON config structure I documented
- But the structure is WRONG/INVALID

### Root Cause:
The JSON structures I wrote in BOOTSTRAP.md for `config.patch` are incorrect. The agent is blindly following my instructions but the config format doesn't match what OpenClaw actually expects.

Example from BOOTSTRAP.md that's probably wrong:
```json
{
  "channels": {
    "telegram": {
      "enabled": true,
      "botToken": "[TOKEN]",
      ...
    }
  }
}
```

### Problems with BOOTSTRAP.md:
1. Config JSON structures are wrong (don't match real schema)
2. `openclaw config schema` doesn't exist as a command
3. Fresh agent has no way to learn correct format
4. When it fails, it tries to read docs that don't exist

---

## BOOTSTRAP.md Rebuild - COMPLETE

### What I Fixed:

**1. Correct Config Paths (verified from actual config.get):**
- Claude API: `models.providers.anthropic.apiKey` + `baseUrl`
- Whisper: `skills.entries.openai-whisper-api.apiKey`
- Telegram: `channels.telegram` with proper structure
- Memory: `agents.defaults.memorySearch`, `compaction.memoryFlush`, `hooks.internal.entries.session-memory`

**2. Forgiving Behavior:**
- Added "Handling Off-Script Questions" section
- Instructions: answer their question, then guide back to onboarding
- Never abandon onboarding for off-topic questions

**3. Progress Tracking:**
- Uses `onboarding-progress.json` to track state
- Stores: currentStep, completed steps, skipped steps, userName
- Resume support: "Welcome back! We left off at..."

**4. Only Delete When Complete:**
- BOOTSTRAP.md only deleted AFTER showing completion summary AND user confirms
- Also deletes progress file at that point

**5. Error Handling:**
- Retry logic
- Graceful skip option if things fail
- Never leave user stuck

**6. Clear EXECUTE Markers:**
- Changed from descriptive to imperative
- "EXECUTE: gateway config.patch" with exact JSON
- Fresh agent knows exactly what to run

### Config Paths Reference (from my system):
```
models.providers.anthropic.apiKey
models.providers.anthropic.baseUrl
skills.entries.openai-whisper-api.apiKey
skills.entries.agentmail.apiKey
channels.telegram.enabled/botToken/dmPolicy/groupPolicy/streamMode
agents.defaults.memorySearch.enabled/sources
agents.defaults.compaction.memoryFlush.enabled
hooks.internal.entries.session-memory.enabled
```

---

## Lessons (Reinforced AGAIN)

- Journal BEFORE the task, not after
- Every conversation, every task - write it down FIRST
- Session memory is NOT reliable - files are

---
